<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Student Management</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet" />
  <style>
    :root {
      --primary-color: #0088b9;
      --secondary-color: #19396e;
      --accent-color: #ffc82e;
      --accent-color-2: #1e8c3f;
      --light-bg: #f0f8ff;
      --text-color: #19396e;
      --light-text: #fff;
      --font-size-base: 16px;
      --font-size-large: 1.5em;
    }

    body {
      font-family: "Poppins", sans-serif;
      background-color: var(--light-bg);
      color: var(--text-color);
      line-height: 1.6;
      margin: 0;
      padding: 0;
      font-size: var(--font-size-base);
    }

    .container {
      width: 90%;
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }

    header,
    footer {
      background-color: var(--primary-color);
      color: var(--light-text);
    }

    .header-strip {
      background-color: var(--primary-color);
      height: 10px;
      border-bottom: 3px solid var(--accent-color);
    }

    header {
      padding: 20px 0;
      text-align: center;
      border-bottom: 4px solid var(--accent-color);
    }

    header h1 {
      margin: 0;
      font-size: 2em;
    }

    header a {
      color: var(--light-text);
      text-decoration: none;
      font-weight: 600;
      font-size: 1em;
    }

    h1,
    h2 {
      margin-top: 20px;
      margin-bottom: 10px;
    }

    h2 {
      color: var(--secondary-color);
      border-bottom: 2px solid var(--accent-color);
      padding-bottom: 5px;
      display: inline-block;
    }

    ul {
      list-style: none;
      padding: 0;
      margin-bottom: 20px;
    }

    li {
      background-color: #fff;
      border: 1px solid #d9edf7;
      border-radius: 4px;
      padding: 15px 20px;
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    li span {
      flex-grow: 1;
    }

    li strong {
      color: var(--secondary-color);
    }

    a.button,
    .back-button {
      background-color: var(--secondary-color);
      color: var(--light-text);
      padding: 8px 16px;
      text-decoration: none;
      border-radius: 4px;
      font-weight: 600;
      transition: background-color 0.3s ease, color 0.3s ease;
      margin-left: 5px;
      cursor: pointer;
      font-size: 0.9em;
      display: inline-block;
    }

    .back-button {
      padding: 10px 20px;
      font-size: 1.1em;
      margin: 0;
    }

    a.button:hover,
    .back-button:hover {
      background-color: var(--accent-color);
      color: var(--secondary-color);
    }

    header a.button {
      background-color: var(--accent-color);
      color: var(--secondary-color);
    }

    header a.button:hover {
      background-color: #fff;
    }

    form {
      background-color: #fff;
      padding: 20px;
      border-radius: 4px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
      border-top: 3px solid var(--accent-color-2);
    }

    form label {
      display: block;
      margin-bottom: 5px;
      font-weight: 600;
      color: var(--secondary-color);
    }

    form input,
    form select {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #d9edf7;
      border-radius: 4px;
    }

    form select[multiple] {
      height: 100px;
    }

    form button {
      background-color: var(--accent-color-2);
      color: var(--light-text);
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1em;
      font-weight: 600;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    form button:hover {
      background-color: var(--accent-color);
      color: var(--secondary-color);
    }

    .search-section {
      display: flex;
      align-items: center;
      margin: 30px 0;
    }

    .search-label {
      font-weight: 600;
      font-size: 1.2em;
      color: var(--secondary-color);
      margin-right: 15px;
    }

    .search-input {
      flex: 1;
      padding: 12px;
      border: 1px solid #d9edf7;
      border-radius: 4px;
      font-size: 1em;
    }

    .top-navigation {
      margin-top: 20px;
    }

    footer {
      background-color: var(--secondary-color);
      color: var(--light-text);
      text-align: center;
      padding: 20px;
      margin-top: 40px;
      border-top: 4px solid var(--accent-color);
    }

    @media (max-width: 768px) {
      header h1 {
        font-size: 1.8em;
      }

      a.button,
      .back-button {
        font-size: 0.85em;
        padding: 7px 14px;
      }

      .back-button {
        padding: 10px 20px;
        font-size: 1em;
      }

      form input,
      form select,
      form button {
        font-size: 0.9em;
      }

      .search-section {
        flex-direction: column;
        align-items: flex-start;
      }

      .search-label {
        margin-bottom: 10px;
      }

      .search-input {
        width: 100%;
      }
    }

    @media (max-width: 480px) {
      header {
        padding: 15px 0;
      }

      header h1 {
        font-size: 1.5em;
      }

      .container {
        padding: 15px;
      }

      li {
        flex-direction: column;
        align-items: flex-start;
      }

      li a.button {
        margin: 5px 0 0 0;
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>

<body>
  <div class="header-strip"></div>
  <header>
    <div class="container">
      <h1>Student Management</h1>
      <a href="/auth/logout" class="button">Logout</a>
    </div>
  </header>
  <div class="container">
    <div class="top-navigation">
      <a href="/admin/dashboard" class="back-button">Back to Dashboard</a>
    </div>

    <div class="search-section">
      <div class="search-label">Search Student:</div>
      <input type="text" id="studentSearch" class="search-input" placeholder="Enter student ID or name" />
    </div>

    <section>
      <h2>All Students</h2>
      <ul id="studentList">
        <% students.forEach(student=> { %>
          <li data-student="<%- JSON.stringify(student) %>">
            <span>
              <strong>
                <%= student.username %>
              </strong>
              <% if (student.name) { %> - <%= student.name %>
                  <% } %>
                    <% if (student.courses && student.courses.length> 0) { %> (Courses: <%= student.courses.join(", ") %>) <% } %>
            </span>
            <a href=" /admin/student/remove/<%=student.username %>" class="button">Remove</a>
                        <a href="/admin/student/edit/<%= student.username %>" class="button">Edit</a>
          </li>
          <% }); %>
      </ul>
    </section>

    <section>
      <h2>Override Registration</h2>
      <form action="/admin/override" method="POST">
        <label for="studentId">Student ID:</label>
        <input type="text" id="studentId" name="studentId" required />
        <label for="courseCode">Course Code:</label>
        <input type="text" id="courseCode" name="courseCode" required />
        <button type="submit" class="btn">Override Registration</button>
      </form>
    </section>
  </div>
  <footer>
    <div class="container">
      <p>&copy; 2025 National University of Sciences. All rights reserved.</p>
    </div>
  </footer>
  <script>
    document
      .getElementById("studentSearch")
      .addEventListener("keyup", function () {
        const query = this.value.toLowerCase();
        const studentItems = document.querySelectorAll("#studentList li");
        studentItems.forEach((item) => {
          const studentData = JSON.parse(item.getAttribute("data-student"));
          const usernameMatch = studentData.username
            .toLowerCase()
            .includes(query);
          const nameMatch = studentData.name
            ? studentData.name.toLowerCase().includes(query)
            : false;
          item.style.display = usernameMatch || nameMatch ? "" : "none";
        });
      });
  </script>
</body>

</html>